#ifndef toolBox
#define toolBox

#include <SDL.h>
#include <string>

namespace toolBoXx
{
    void toolBoxMain(SDL_Renderer* renderer);
    void toolBoxFunc(std::string MODE, SDL_Event event);
}

#endif
